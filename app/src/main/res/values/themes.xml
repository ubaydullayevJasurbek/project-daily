<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DailyOutComing" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryVariant</item>
        <item name="colorSecondary">@color/colorSecondary</item>
        <item name="colorSecondaryVariant">@color/colorSecondaryVariant</item>

        <item name="android:windowBackground">@color/background</item>
        <item name="android:textColorPrimary">@color/textPrimary</item>
        <item name="android:textColorSecondary">@color/textSecondary</item>
        <item name="android:statusBarColor">@color/black</item>
        <item name="enableEdgeToEdge">false</item>

    </style>

    <style name="MyMaterialDatePickerTheme" parent="ThemeOverlay.MaterialComponents.MaterialCalendar">
        <!-- Header rang -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorOnPrimary">@android:color/white</item>

        <!-- Dialog fon rangi -->
        <item name="android:windowBackground">@android:color/white</item>

        <!-- Sana matni rangi -->
        <item name="android:textColorPrimary">@android:color/black</item>

        <!-- Tugma ranglari -->
        <item name="materialCalendarHeaderConfirmButton">@color/primary</item>
        <item name="materialCalendarHeaderCancelButton">@color/gray</item>

        <!-- Header matni -->
        <item name="materialCalendarHeaderTitle">@android:color/white</item>
    </style>

    <style name="Theme.DailyOutComing" parent="Base.Theme.DailyOutComing" />
    
    <style name="AppTheme.Light" parent="Theme.Material3.Light">

    </style>

    <style name="AppTheme.Dark" parent="Theme.Material3.Light">

    </style>

    <style name="AppTheme" parent="Theme.AppCompat.DayNight">

    </style>
</resources>